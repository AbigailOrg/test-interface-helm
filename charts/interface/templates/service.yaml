apiVersion: v1
kind: Service
metadata:
  name: {{ include "interface-test.fullname" . }}
  labels:
    {{- include "interface-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "interface-test.selectorLabels" . | nindent 4 }}
